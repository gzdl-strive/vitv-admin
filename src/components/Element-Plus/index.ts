import TableList from './TableList';
import { ComponentOption } from './typing';

const components: ComponentOption[] = [
  {
    name: 'table-list',
    component: TableList
  }
];

export default components;